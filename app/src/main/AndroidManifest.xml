<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Cloud AI (OpenRouter) calls. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Show reminder notifications (Android 13+ runtime permission). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Exact reminder alarms. USE_EXACT_ALARM is auto-granted at install for genuine
         calendar/alarm apps (Play-policy gated). Required by setAlarmClock on Android 16. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Re-arm reminders after device reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Read the device's existing calendars (Google/Samsung) to live alongside them. -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <application
        android:name=".AiCalendarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AiCalendar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AiCalendar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Fired by AlarmManager at reminder time (explicit PendingIntent → not exported). -->
        <receiver
            android:name=".data.reminder.ReminderReceiver"
            android:exported="false" />

        <!-- Re-arms reminders after reboot (protected BOOT_COMPLETED broadcast). -->
        <receiver
            android:name=".data.reminder.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
